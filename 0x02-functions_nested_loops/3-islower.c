#include <stdio.h>
#include <ctype.h>

/**
 * _islower - funtion to check for lowercase
 * @c: input
 *
 * Return: 0 or 1
 */
int _islower(int c)
{
	if (c == islower())
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
